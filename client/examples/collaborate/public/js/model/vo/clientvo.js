function ClientVO(clientId, mouseX, mouseY, comment, isDrag) {
    this.clientId = clientId;
    this.mouseX = mouseX;
    this.mouseY = mouseY;
    this.comment = comment;
    this.isDrag = isDrag;
}